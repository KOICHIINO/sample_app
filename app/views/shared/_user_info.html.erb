<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "プロフィール", current_user %></span>
<span>投稿数：<%= current_user.microposts.count %></span>


<!--以下、複数形にするメソッドのコメントアウト-->
<% if false %><span><%= pluralize(current_user.microposts.count, "micropost") %></span><% end %>